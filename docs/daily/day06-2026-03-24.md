# 6일차 일지 (2026-03-24)

## 업무 요약
- MySQL DB 연결 문제 해결 예정 (백엔드 서버 정상 실행 목표)
- 프론트엔드 Axios 클라이언트 모듈 설정 (`lib/api/client.ts`)
- 프론트엔드 레이아웃 컴포넌트 및 메인 페이지 UI 구현 예정

## 이슈/해결
- **MySQL learnhub@localhost 계정 비밀번호 불일치 문제 (Day 5 이어서)**
  - 해결: `docker exec learnhub-mysql mysql -uroot -plearnhub1234 -e "ALTER USER 'learnhub'@'localhost' IDENTIFIED WITH mysql_native_password BY 'learnhub1234';"` 실행으로 비밀번호 통일

## 학습 포인트
- Next.js App Router에서 Axios 클라이언트를 `lib/api/client.ts`에 싱글톤으로 분리하면 baseURL, timeout, 헤더 설정을 한 곳에서 관리 가능
- `process.env.NEXT_PUBLIC_API_URL` 환경변수로 개발/운영 환경별 API URL 분리

## 내일의 계획
- 강의 목록 페이지 구현 (백엔드 API 연동)
- 강의 상세 페이지 구현 (백엔드 API 연동)

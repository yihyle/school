# 3일차 일지 (2026-03-21)

## 업무 요약
- 백엔드 도메인 패키지 구조 생성 (course, section, lecture, enrollment, progress, user, global)
- 공통 응답 래퍼 ApiResponse, 커스텀 예외 AppException, GlobalExceptionHandler 구현
- Course CRUD API 구현 (Entity, Repository, Service, Controller) - 목록 조회 / 카테고리 필터 / 검색 / 인기 강의 / 상세 조회 / 생성 / 수정 / 삭제

## 이슈/해결
- 없음

## 학습 포인트
- Spring Boot 도메인 중심 패키지 구조 (course/, section/ 등) 적용 - 계층 중심(controller/, service/) 구조보다 도메인별로 묶는 방식이 유지보수에 유리
- `@RestControllerAdvice`로 전역 예외 처리 표준화 - 모든 컨트롤러에서 try-catch 없이 일관된 에러 응답 반환 가능
- JPA `@PrePersist` / `@PreUpdate`로 createdAt / updatedAt 자동 관리

## 내일의 계획
- Section CRUD API 구현
- Lecture CRUD API 구현
